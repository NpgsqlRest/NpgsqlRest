# Changelog

The changelog is split per version under [`changelog/`](./changelog/) — one file per release (`vX.Y.Z.md`).

Pre-3.0 versions are archived in [`changelog/legacy.md`](./changelog/legacy.md).
